# Session Note 2026-05-22

## Muc tieu

Xu ly va dieu tra cac van de deploy/monitor tren VPS cho `ozon_telegram_bot`, gom:

- dang nhap VPS
- xem log worker/scheduler
- sua Ozon `client_id` / `api_key` cho tung shop
- sua Lark webhook
- dieu tra ly do bot bo sot return moi cua `sensia02`
- them script deploy local -> VPS
- sua loi `scheduler` tranh cong voi `command_server`

## Thong tin VPS

- VPS IP: `103.153.254.116`
- SSH:

```bash
ssh root@103.153.254.116
```

## Lenh quan trong da dung

### Log

```bash
journalctl -u ozon-worker -f
journalctl -u ozon-scheduler -f
journalctl -u ozon-command-server -f
```

Loc loi:

```bash
journalctl -u ozon-worker -f | grep -E "ERROR|WARNING|Invalid Api-Key|Lark|Traceback"
```

### Vao PostgreSQL tren VPS

```bash
export DATABASE_URL='postgresql://ozon_app:123456789abcXYZ!@127.0.0.1:5432/ozon_bot'
psql "$DATABASE_URL"
```

### Kiem tra shop / credential / channel

```sql
SELECT * FROM shops WHERE code = 'world17_3';

SELECT *
FROM shop_credentials
WHERE shop_id = 7;

SELECT *
FROM shop_channels
WHERE shop_id = 6
  AND channel_type = 'lark';
```

## Cac van de da xu ly

### 1. Ozon credential sai o nhieu shop

Da sua/cap nhat key cho cac shop bi `Invalid Api-Key` trong log:

- `world17_3`
- `world17`
- `world17_2`
- `sensia01`
- `sensia02`
- `turbotime`
- `sportvibe_apparel_01`

Vi du update:

```sql
UPDATE shop_credentials
SET api_key = 'a9a19a37-aa4b-4999-8073-d338e0877193',
    is_active = TRUE,
    updated_at = NOW()
WHERE shop_id = 7
  AND provider = 'ozon';
```

### 2. Lark webhook sai

Da sua webhook cho:

- `world17_3`
- `world17_2`

Vi du:

```sql
UPDATE shop_channels
SET target = 'https://open.larksuite.com/open-apis/bot/v2/hook/ea9fa1d9-80f5-42b0-8f91-ee9570b5e487',
    is_active = TRUE,
    updated_at = NOW()
WHERE shop_id = 6
  AND channel_type = 'lark';
```

### 3. Dieu tra bug bo sot return moi cua `sensia02`

Case can theo doi:

- shop: `sensia02`
- return number: `81694830-R6`
- posting number: `81694830-0408-1`

Ket qua dieu tra:

- API Ozon tra ve du lieu return moi
- bot khong notify khong phai do thieu quyen API
- cung khong phai do Lark loi
- nguyen nhan la logic lay returns cu: chi cat `limit=20`, de sot don moi nam cuoi danh sach RFBS

Da test tay:

```bash
curl -X POST "$OZON_BASE_URL/v2/returns/rfbs/list" \
  -H "Client-Id: $OZON_CLIENT_ID" \
  -H "Api-Key: $OZON_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "filter": {},
    "limit": 100,
    "last_id": 0
  }'
```

Response co chua return:

- `81694830-R6`

## Code local da sua

### 1. Fix bot bo sot return moi

File:

- [integrations/ozon_client.py](./integrations/ozon_client.py)

Noi dung fix:

- tang pham vi tim `returns`
- sort theo `created_date` moi nhat truoc khi cat ve `limit`
- ap dung cho ca FBS va RFBS flow

### 2. Them test hoi quy

File:

- [tests/test_ozon_client.py](./tests/test_ozon_client.py)

Muc dich:

- bat case don moi nam cuoi page nhung van phai duoc lay

### 3. Fix `scheduler` tranh cong HTTP voi `command_server`

Nguyen nhan:

- `Container()` luon khoi tao `CommandServer`
- `ozon-scheduler` khi start cung bind HTTP port
- gay loi:

```text
OSError: [Errno 98] Address already in use
```

File da sua:

- [app/bootstrap.py](./app/bootstrap.py)
- [scripts/run_scheduler.py](./scripts/run_scheduler.py)
- [scripts/run_worker.py](./scripts/run_worker.py)

Huong fix:

- them `enable_command_server`
- `scheduler` va `worker` khoi tao `Container(..., enable_command_server=False)`
- chi `command_server` / `main.py` moi mo HTTP port

## Script deploy local -> VPS

Da them:

- [scripts/deploy_to_vps.ps1](./scripts/deploy_to_vps.ps1)

Chay tren may local Windows:

```powershell
cd "C:\Users\thang\OneDrive\Máy tính\tool mmo v2\ozon_telegram_bot"
powershell -ExecutionPolicy Bypass -File .\scripts\deploy_to_vps.ps1
```

Neu chi muon copy code:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\deploy_to_vps.ps1 -SkipTests -SkipRestart
```

Luu y:

- khong chay file `.ps1` tren VPS Linux

## Kiem tra sau deploy

### Xac nhan patch returns da co tren VPS

```bash
grep -n "search_limit" integrations/ozon_client.py
grep -n "_sort_returns_by_created_desc" integrations/ozon_client.py
```

### Restart service

```bash
systemctl reset-failed ozon-scheduler
systemctl restart ozon-worker ozon-scheduler ozon-command-server
```

### Kiem tra return moi da vao DB chua

```sql
SELECT shop_id, return_number, last_status, last_notified_status, updated_at
FROM return_state
WHERE shop_id = 10
  AND return_number = '81694830-R6';
```

## Ket luan buoi lam viec

- da sua Ozon key va Lark webhook cho cac shop loi
- da tim ra bug bo sot return moi
- da fix bug returns tren code local
- da tim ra va fix them loi `scheduler` tranh cong voi `command_server`
- da them script deploy local -> VPS

## Viec can theo doi tiep

- sau moi lan deploy, kiem tra `ozon-scheduler` co active khong
- theo doi `return_state` va notify Lark cho `sensia02`
- neu can deploy chuan hon ve sau, co the doi sang workflow git thay vi source copy roi
